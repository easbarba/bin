#!/usr/bin/env bash

# DEBUG SETTINGS
set -euo pipefail

s-dev-emacs
s-dev-ruby
s-dev-javascript
s-dev-scheme
s-dev-python
s-ops-containers

# * USER
if [[ -x $(command -v qas) ]]; then
    qas --grab
    qas --archive bin,lar,annotations # TODO: save to usb-stick
fi

exit
