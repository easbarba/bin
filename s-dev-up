#!/usr/bin/env bash

# DEBUG SETTINGS
set -euo pipefail

# * DEVELOPMENT
s-dev-emacs
s-dev-javascript
s-dev-scheme
s-dev-ruby
s-dev-python
s-ops-containers

# * USER
if [[ -x $(command -v qas) ]]; then
    qas --grab
    qas --archive bin,lar,annotations # TODO: save to usb-stick
fi

